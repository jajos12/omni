model:
  encoder_layers: 6
  decoder_layers: 3
  gradient_checkpointing: true

data:
  max_frames: 160
  num_workers: 2

training:
  batch_per_gpu: 1
  validation_batch_size: 1
  validation_max_batches: 8
  log_every: 10

inference:
  batch_size: 1
  beam_width: 1
